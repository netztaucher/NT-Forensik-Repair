# Forensische Läufe / Belege NIEMALS ins Repo (enthalten echte Kundendaten)
forensik/
wartungsscripte/
*_server/
*_server.tar.gz
belege/
lauf.log
*.tar.gz

# Nur die maskierten Beispiele in examples/ sind erlaubt.

# OS / Editor
.DS_Store
*.swp
*~

# Lokale Lauf-Kopien (Kundendaten)
laeufe/

# Repair-Laufordner (Protokolle, Prüfsummen, echte Kundenpfade)
repair/

# Serena (LSP-Sitzungsdaten, lokal)
.serena/

# Handover-Mechanik (lokale Logs)
logs/

# Arbeitsdateien der Datenpflege (werkzeuge/joomla-daten-update.sh)
.vel_offen.tsv

# Python-Zwischenstände (py_compile in der CI, Selbsttest lokal)
__pycache__/
*.pyc

# Fremde Regelsaetze (LGPL) — vor Ort geholt, nicht mitgeliefert
signaturen/fremd/

# Backlog-Ablage (Historien-Rewrite-Vorbereitung, unbereinigte Kundenpfade, Bundles) — lokal, nie ins oeffentliche Repo
backlog/
